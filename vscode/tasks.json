{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "python format with trailing comma",
            "type": "shell",
            "command": "source ${HOME}/.zshrc; add-trailing-comma ${file}; black ${file}; add-trailing-comma ${file}; black ${file}",
            "presentation": {
                "reveal": "never",
                "echo": false,
                "panel": "shared"
            }
        }
    ]
}

